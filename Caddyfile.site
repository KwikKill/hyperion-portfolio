gabriel.{$WEBSITE_HOST} {
	import cors

	root * /srv/infra/volumes/hyperion
	try_files {path} /index.html

	handle /assets/* {
		header Cache-Control "public, immutable, max-age=31536000"
		file_server
	}

	handle {
		file_server
	}
}
